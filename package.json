{
  "name": "students-calendar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "live-server --port=5500 --no-browser",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
